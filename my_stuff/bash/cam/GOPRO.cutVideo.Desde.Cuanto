#!/bin/bash
# ffmpeg -ss 00:16:52.1 -i GOPR4301.MP4 -c copy -t 00:00:49.0 going.out.mp4
# GOPRO.cutSarasa ARCHIVO.MP4 HH:MM:SS.m HH:MM:SS.m
ext=${1##*.}
rm output.${ext}
ffmpeg -ss $2 -i "$1" -c copy -t $3 output.${ext}
mpv output.${ext}
